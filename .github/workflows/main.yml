name: Generate Pacman Contribution Graph

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: echo "Workflow works"
